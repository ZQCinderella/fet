http://nodejs.org
node文件操作
	step.1 加载fs模块
	step.2 操作文件的读/写
		   有两种读写文件的方法：同步和异步
		   readFile();
		   readFileSync();
	step.3 怎么写文件
			同步和异步
			writeFile();
			writeFileSync();

nodejs的http服务的创建
	掌握url模块的使用
		step.1 加载url模块
		parse
		format

		step.2 加载http模块
		1).了解请求clientRequest，响应serverResponse
		2).了解服务器对象
			step.1创建服务器对象